{
  "name": "esbuild",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
  "build": "esbuild src/index.tsx --bundle --minify --sourcemap --define:process.env.NODE_ENV=\\\"production\\\" --outfile=dist/bundle.js && cp public/index.html dist/index.html",
    "build:dev": "node esbuild.dev.js",
    "start": "node esbuild.config.js -w",
    "open": "open public/index.html",
    "deploy": "gh-pages -d dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "esbuild-serve": "^1.0.1",
    "gh-pages": "^6.3.0"
  }
}
